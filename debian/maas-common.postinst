#!/bin/sh

set -e

add_user_group(){
	local user="maas"
	local group="maas"
	addgroup --quiet --system "$group" || true
	adduser --quiet --system --group --no-create-home "$user" || true
}

add_user_group

#DEBHELPER#
